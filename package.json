{
  "name": "github-actions",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vc --prod --token=$VERCEL_TOKEN"
  },
  "devDependencies": {
    "vercel": "^28.13.1",
    "vite": "^4.0.0"
  }
}
